{
  "name": "calpro-plus",
  "version": "2.29.1",
  "description": "CalPro+ — calorie tracking + TDEE calculator (Thai/English PWA wrapped via Capacitor)",
  "private": true,
  "scripts": {
    "prepare:www": "node scripts/prepare-www.js",
    "cap:init": "cap init CalPro+ com.calpro.app --web-dir=www",
    "cap:add-android": "cap add android",
    "cap:add-ios": "cap add ios",
    "cap:sync": "cap sync",
    "assets:generate": "capacitor-assets generate --android",
    "build:android-debug": "npm run prepare:www && cap sync android && cd android && ./gradlew assembleDebug",
    "build:android-release": "npm run prepare:www && cap sync android && cd android && ./gradlew bundleRelease",
    "build:ios": "npm run prepare:www && cap sync ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/core": "^8.3.4",
    "@capacitor/ios": "^8.3.4",
    "@capacitor/status-bar": "^8.0.2",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/local-notifications": "^8.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.4",
    "@capacitor/assets": "^3.0.5"
  }
}
